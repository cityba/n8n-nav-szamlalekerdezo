import { Logger } from '@n8n/backend-common';
import { CronLoggingConfig } from '@n8n/config';
import { CronJob } from 'cron';
import type { Cron, Workflow } from 'n8n-workflow';
import { InstanceSettings } from '../instance-settings';
export declare class ScheduledTaskManager {
    private readonly instanceSettings;
    private readonly logger;
    private readonly config;
    readonly cronMap: Map<string, {
        job: CronJob;
        displayableCron: string;
    }[]>;
    private readonly logInterval?;
    constructor(instanceSettings: InstanceSettings, logger: Logger, config: CronLoggingConfig);
    private logActiveCrons;
    registerCron(workflow: Workflow, { expression, recurrence }: Cron, onTick: () => void): void;
    deregisterCrons(workflowId: string): void;
    deregisterAllCrons(): void;
}
